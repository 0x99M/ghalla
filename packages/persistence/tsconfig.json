{
  "//": [
    "exactOptionalPropertyTypes is OFF here and nowhere else: Drizzle's inferred",
    "insert types use `field?: T` rather than `field?: T | undefined`. It stays ON",
    "in contracts, where it is what makes 'absence is | null, never ?' enforceable."
  ],
  "extends": "../../tooling/tsconfig/base.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist",
    "tsBuildInfoFile": "./dist/.tsbuildinfo",
    "types": [
      "node"
    ],
    "exactOptionalPropertyTypes": false
  },
  "include": [
    "src/**/*.ts"
  ],
  "references": [
    {
      "path": "../contracts"
    },
    {
      "path": "../core"
    },
    {
      "path": "../billing"
    }
  ]
}
