/** Scaffold. Proves the app builds and serves; the overview lands with the read routes. */
export default function HomePage() {
  return (
    <main>
      <h1>Ghalla Ops</h1>
      <p>Internal operator portal.</p>
    </main>
  );
}
